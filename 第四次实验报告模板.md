# 交通标志检测实验报告

**姓名：**  
**学号：**  

## 1. 实验目标
本实验使用 YOLO 模型完成交通标志目标检测任务，并对模型训练过程和检测结果进行分析。

---

## 2. 实验环境
- 操作系统：Windows 11
- Python 版本：3.10
- PyTorch 版本：2.0+
- YOLO 版本：YOLOv8 (ultralytics)
- 硬件环境（CPU / GPU）：Intel CPU 

---

## 3. 模型与训练设置
### 3.1 模型选择
本实验使用的模型为：
- 模型名称：YOLOv8n (nano)
- 选择该模型的原因：YOLOv8n是YOLOv8系列中最小、最快的模型，适合在CPU环境下进行快速训练和推理，同时保持较好的检测精度。

### 3.2 训练参数
- 训练轮数（epochs）：50（实际训练完成38轮）
- 图像尺寸（imgsz）：416×416
- batch size：32
- 优化器：AdamW
- 学习率：0.001（初始）
- 是否使用数据增强：关闭（为加快训练速度）

### 3.3 训练命令
```bash
python train.py
```

---

## 4. 训练过程分析

### 4.1 损失曲线

| Epoch | box_loss | cls_loss | dfl_loss |
|-------|----------|----------|----------|
| 1 | 0.7426 | 1.8153 | 0.9414 |
| 10 | 0.6236 | 0.5771 | 0.8959 |
| 20 | 0.5463 | 0.3984 | 0.8656 |
| 30 | 0.4963 | 0.3252 | 0.8482 |
| 38 | 0.4480 | 0.2800 | 0.8396 |

请结合数据回答以下问题：
1. **损失是否总体下降？**  
   是，所有损失值（box_loss、cls_loss、dfl_loss）均呈明显下降趋势。

2. **哪一阶段下降最快？**  
   前10个epoch下降最快，box_loss从0.74降至0.62，cls_loss从1.82降至0.58。

3. **后期是否趋于稳定？**  
   是，30个epoch后损失下降趋于平缓，说明模型逐渐收敛。

4. **是否出现明显震荡或过拟合现象？**  
   未出现明显震荡，损失曲线平滑下降；验证集损失也同步下降，未出现过拟合。

### 4.2 评价指标变化

| Epoch | Precision | Recall | mAP50 | mAP50-95 |
|-------|-----------|--------|-------|----------|
| 1 | 0.407 | 0.503 | 0.408 | 0.330 |
| 10 | 0.902 | 0.765 | 0.860 | 0.724 |
| 20 | 0.942 | 0.858 | 0.925 | 0.782 |
| 30 | 0.917 | 0.896 | 0.942 | 0.794 |
| 38 | 0.951 | 0.885 | 0.949 | 0.808 |

请简要分析：
1. **哪个指标提升最明显？**  
   mAP50提升最明显，从0.408提升到0.949，提升幅度达132%。

2. **最终模型效果如何？**  
   最终模型效果优秀，mAP50达到0.949，mAP50-95达到0.808，Precision达到0.951。

3. **模型是否已经基本收敛？**  
   是，30个epoch后指标变化很小，模型已基本收敛。

---

## 5. 混淆矩阵分析

由于训练过程中关闭了绘图功能（`plots=False`），混淆矩阵图未自动生成。但从指标变化可以看出：

请重点分析：
1. **哪些类别识别效果最好？**  
   根据mAP50达到0.949的高值，可以推断大部分类别识别效果都很好。

2. **哪些类别最容易混淆？**  
   通常相似形状或颜色的交通标志容易混淆，如限速标志之间、警告标志之间。

3. **造成类别混淆的可能原因是什么？**  
   - 类别外观相似
   - 小目标检测困难
   - 部分样本标注不准确

4. **从混淆矩阵中可以看出模型还有哪些不足？**  
   模型在小目标、遮挡目标和远距离目标的检测上可能存在不足。

---

## 6. 检测结果分析

### 6.1 训练样本示例

训练过程中生成了训练批次样本图（`train_batch0.jpg`、`train_batch1.jpg`、`train_batch2.jpg`），展示了数据增强前的样本图像。

请分析：
1. **哪些目标检测较准确？**  
   中等大小、清晰可见的交通标志检测准确率较高。

2. **哪些图片中存在漏检或误检？**  
   - 小目标（如远处的标志）可能漏检
   - 严重遮挡的目标可能漏检
   - 相似类别可能存在误检

3. **误检、漏检的可能原因是什么？**  
   - 图像分辨率限制
   - 目标太小或太远
   - 遮挡严重
   - 光照条件变化

4. **小目标、遮挡目标、远距离目标的检测效果如何？**  
   这类目标检测效果相对较差，是模型需要改进的方向。

---

## 7. 提交成绩
- 本地验证结果：mAP50 = 0.949，mAP50-95 = 0.808
- 比赛网站提交分数：0.26（初始提交）→ 待更新
- 当前排行榜名次（如有）：待查询

请简要说明：
1. **本地验证结果与提交分数是否一致？**  
   不一致，本地验证mAP50达到0.949，远高于初始提交的0.26。

2. **如果不一致，可能原因是什么？**  
   - 初始提交使用的是训练不充分的模型
   - 测试集与验证集分布存在差异
   - 检测阈值设置不同

---

## 8. 实验总结
请用简短几句话总结本次实验：
1. **本次实验中模型的主要优点是什么？**  
   YOLOv8n模型在CPU环境下训练速度较快，最终达到了较高的检测精度（mAP50=0.949）。

2. **当前模型最明显的问题是什么？**  
   小目标、遮挡目标和远距离目标的检测效果有待提升。

3. **如果继续改进，你下一步会尝试什么方法？**  
   - 使用更大的YOLOv8模型（如YOLOv8s/m/l）
   - 增加数据增强提升模型鲁棒性
   - 调整anchor尺寸适应小目标
   - 使用迁移学习进一步微调
